# ProjectPipeline Product Outcome

The terminal product is a continuously operating, local-first autonomous engineering organization. Its authoritative machine contract is [config/product_outcome.json](../../config/product_outcome.json), grounded in `SRC-014:L000001-L000086` and `SRC-015:L000001-L000112` from `INPUT-KNOWLEDGE-001`.

The observable loop is:

`project input → verified project model → autonomous selection → conflict-safe execution → verification → governed repository integration → Jira reconciliation → project-state recomputation → next ready work → repeat`

A scoped `HUMAN_REQUIRED` incident blocks its dependent subgraph while every unaffected lane continues. The Command Center reports this live truth and allows governed operator interaction; it is not the autonomy runtime or completion authority.

The outcome remains incomplete until the full ladder passes: deterministic contracts, local-real integrated journey, isolated real Git/worktree journey, authorized GitHub/Jira qualification, real worker/provider dispatch, Windows service and Command Center, restart/recovery, 24-hour unattended, 72-hour unattended, release/post-release verification, and the deterministic Completion Gate.

Classes, schemas, plans, lifecycle transitions, component composition, deterministic simulations, mocked remotes, generated evidence, or a local-only preview are useful intermediate evidence but cannot qualify the terminal product.

