# Infrastructure capability boundaries

Provider-neutral local adapters cover the private overlay, ephemeral integration
dependencies, optional cloud emulation, authenticated event ingress, and capacity
requests. Paid cloud spend is not required to prove the interfaces. Emulation
cannot substitute for required live validation. Ingress never grants direct
control authority.
