# Architecture Decisions

Accepted structural and technology choices are recorded with stable identifiers, exact source basis, alternatives, consequences, affected requirements, affected components, and review triggers.

`ADR_CATALOG.json` is the machine-readable authority. `architecture/decision_map.json` adds technology mappings. A selection does not claim that the corresponding runtime integration or live environment is complete.
