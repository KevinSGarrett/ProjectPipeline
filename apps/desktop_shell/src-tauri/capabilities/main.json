{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal Command Center desktop capability set. Canonical project mutation remains HTTP control-plane work.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
