# PPQS-06 r02 Project Pack Expansion Notes

This candidate-visible pack preserves the r01 project inputs and adds a standalone manifest, deterministic seed-Jira representation, runtime contract, toolchain lock, visible provenance catalog, acquisition lock where applicable, and a candidate-safe self-check CLI.

The private Gold Requirements, Gold Jira, hidden tests, reference targets, scoring, and hard gates are intentionally absent.
