# Inspector Release Advancement Request

The supplied repository is frozen at release `2.1.0`. Produce the next qualified release by implementing the visible feature and quality requests in this pack. Preserve the existing web, CLI, TUI, shared-core, test-server, package, and migration behavior unless a visible requirement explicitly changes it.

Do not search for, fetch, compare against, or infer the hidden target release. The evaluator scores behavior, compatibility, packaging, tests, and evidence. A release that only changes version metadata does not pass.
