# Visible Release Validation

The candidate release must run the repository's documented validation, coverage, build-gate, smoke, Storybook/Playwright, pack verification, and clean-install package tests. It must add focused tests for every release request and eliminate timing sleeps or skipped checks used only to obtain green CI.
